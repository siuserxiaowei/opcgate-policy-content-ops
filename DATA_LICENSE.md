# 数据与使用边界

- `data/opcgate-policies.json`、`data/cities.json` 和 `data/communities.json` 是 OPC Gate 的 2026-05-22 数据快照；它们不是实时政策或全国完整名录。
- `data/policies.json` 和 `data/sample-topics.json` 是既有 JavaScript 原型的最小回归测试夹具。
- 政策原文不随代码许可证重新授权；使用者必须回到发布机构最新页面核验。
- 用户手动粘贴的文本只在当前应用请求中处理，本项目没有数据库写入或自动采集功能。
- 比赛版推荐逻辑在本地运行，不向第三方模型发送创业项目描述；用户主动打开载体来源或公开申请入口时，由目标网站按其条款处理访问。
- 不得输入私信、Cookie、Token、账号凭据或未授权个人信息。
- 输出是内容研究辅助，不是政务办理、法律意见、申报资格认定或获批预测。

{
  "name": "opcgate-policy-content-ops",
  "version": "1.0.0",
  "private": false,
  "description": "Evidence-aware policy content operations assistant for ModelScope AI+Operations contest.",
  "type": "module",
  "scripts": {
    "demo": "node scripts/demo.mjs",
    "test": "node --test tests/*.test.mjs",
    "test:python": "python3 -m unittest discover -s tests -p 'test_*.py' -v",
    "capture:submission": "python3 scripts/capture_submission.py",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "license": "MIT",
  "engines": {
    "node": ">=20"
  }
}
